version: 0.4.2
name: deno_es
scripts:
  conn:
    cmd: deno run -A examples/conn.ts
    watch: false    
  log: 
    cmd: conventional-changelog -p angular -i CHANGELOG.md -s -r 0
    watch: false    